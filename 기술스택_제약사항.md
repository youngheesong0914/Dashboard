# 기술 스택 제약사항 (Coding Agent 전달용) · v2

> 이 문서의 제약은 **선호가 아니라 환경상 불가능**을 의미한다. 위반 시 배포 자체가 안 된다.
> v2 변경: 차트 스택 Chart.js → **Vega-Lite** 확정 반영 (아키텍처 v5 / 동작 시나리오 v3와 정합). 잔존하던 Chart.js·chart.update() 표기 제거.

## 1. 사용 가능한 것 (이것이 전부)

| 항목 | 내용 |
|---|---|
| 서버 | Tomcat - **JSP 파일 배포만 가능** |
| 서버 로직 | JSP 내 Java 코드 (스크립틀릿/선언부). JSP에서 외부 HTTP API 호출 가능 |
| 클라이언트 | JSP가 출력하는 HTML/CSS/JS (브라우저 실행, 순수 Vanilla JS) |
| 차트 | **Vega-Lite** (vega + vega-lite + vega-embed 3종) - CDN 우선, 차단 시 3개 파일 로컬 배포(`static/js/`). 차트 spec은 서버(외부 API, 목업 단계에서는 프록시 JSP)가 완성해 내려주며 프론트는 vegaEmbed 렌더링만 |

## 2. 사용 불가 (금지 목록)

- 별도 백엔드 서버: FastAPI, Flask, Spring Boot, Node.js 등 **일체 불가**
- 별도 서블릿(.java) 컴파일 배포 불가 - 서버 로직은 **JSP 파일 안에서만**
- Nginx 등 리버스 프록시 불가
- Redis 등 외부 캐시/저장소 불가 - 캐시가 필요하면 JSP `application` 스코프(서버 메모리)만 사용
- npm / webpack / vite 등 **빌드 도구 일체 불가** - JS는 빌드 없이 브라우저에서 바로 실행되는 코드만
- React / Vue / jQuery 등 프레임워크·라이브러리 도입 금지 - **순수(Vanilla) JS + Vega 3종(vega/vega-lite/vega-embed)만**. Chart.js는 사용하지 않음(폐기)
- DB 직접 접속 불가 - 데이터는 오직 외부 데이터 API를 통해서만 획득

## 3. 아직 정해지지 않은 것 (TBD - 임의로 확정하지 말 것)

| 항목 | 상태 | 에이전트가 할 일 |
|---|---|---|
| 외부 데이터 API | 엔드포인트·인증 미정. 단 **응답에 위젯별 Vega-Lite spec을 완성해 포함하기로 확정** (테이블은 원시 행 배열) | 호출부를 apiCommon.jsp 한 곳에 모아 교체 가능하게 분리. 실제 호출 대신 화면 명세 샘플 데이터로 **spec 형태의 목업 JSON**을 반환하게 구현 |
| 차트 클릭 이벤트 필드 정의 (stage/risk 등) | 미정 - API 명세에 포함 예정 | 목업 spec에 잠정 필드로 구현하고 TODO 주석 |
| AI Insight 요약 생성 주체 | 미정 (외부 API 포함? 별도 LLM 엔드포인트?) | proxyClaimInsight.jsp가 화면 명세의 aiSummary 텍스트를 목업 반환. TODO 주석 |
| Open report 클릭 동작 | 미정 | 버튼·핸들러 지점만 구현, TODO 주석 |
| Raw Data Download 파일 생성 | 미정 | 호출 지점만 구현, TODO 주석 |
| 로그인/세션 연동 방식 | 미정 | 프록시 JSP 첫 부분에 세션 체크 지점만 구현, TODO 주석 |

## 4. 구조 요약 (아키텍처 문서 v5 참조)

```
브라우저(HTML+JS + vega/vega-lite/vega-embed) ↔ 화면 JSP(queue/claimLT) / 프록시 JSP(spec 포함 JSON 반환) ↔ [TBD] 외부 데이터 API
```

- 화면 JSP: 레이아웃 + 위젯 컨테이너 + 내장 JS
- 프록시 JSP: 세션 체크 후 외부 API 중계(JSON 패스스루, **위젯별 Vega-Lite spec 포함**) - API 미정이므로 당장은 목업 JSON 반환
- 브라우저가 외부 API를 직접 호출하지 않는다 (키 노출·CORS·변경 대응)

## 5. 코드 작성 규칙

1. 산출물 파일은 전부 `.jsp` (+ 정적 css/js 허용 여부는 배포 환경 확인 후 - Vega 3종 로컬 배포가 필요할 수 있음)
2. 페이지 리로드 방식이 아니라 **fetch + 재렌더** 방식: 차트는 새 spec으로 vegaEmbed 재실행, 테이블은 tbody 재생성 (동작 시나리오 문서 참조)
3. 프론트는 **수신한 Vega spec을 수정하지 않는다** - 컨테이너 크기 등 vegaEmbed 옵션 수준만 허용
4. 외부 API 키·토큰을 JSP 출력(HTML/JS)에 포함하지 말 것 - 서버 측 코드에만
5. 신규 대시보드 추가 = 화면 JSP 1개 + 프록시 JSP 1개 + 사이드바 메뉴 1줄 패턴을 유지할 것
