# Sales-Deduction AI Agent - Approver Dashboard Hub · 구현 아키텍처 (v5.1)

기준 문서: **화면 명세 (Hub Navigation · Screen Spec, mid-fi)** - 충돌 시 데이터·동작은 본 문서, 레이아웃·스타일은 화면 명세 우선.
환경 제약: **Tomcat에 JSP 파일 배포 + JSP에서 외부 데이터 API 호출**만 가능 (기술스택_제약사항.md 참조)

> **v5 확정 사항: 차트는 Vega-Lite. 외부 API가 "Vega-Lite spec까지 완성해서" 반환하기로 합의됨.**
> 백엔드가 그리는 방법(spec)을 전부 정하고, 프론트는 렌더링만 한다.
> v5.1 (정합성 패치): §4.2에 Claim 월 선택기 범위 명시, §4.3에 "월 변경 시 재요청 없음" 명시 (동작 시나리오 v3.1과 정합).

---

## 1. 전체 구성

```
[브라우저]                        [Tomcat - JSP 파일만]            [외부 데이터 API]
 JSP가 출력한 HTML+JS
 + vega/vega-lite/vega-embed
   │
   │ ① 화면 요청 ───────────────▶ queue.jsp / claimLT.jsp (화면용)
   │
   │ ② fetch('proxy/xxx.jsp?      ▶ ③ 프록시 JSP: 세션 체크 →
   │        month=2026-06&...')        외부 API 중계 (패스스루)  ──▶ 데이터 조회
   │                                                                 + Vega spec 조립
   │ ⑤ vegaEmbed(spec)             ◀─ ④ spec 포함 JSON 그대로 반환 ◀──
   │    테이블은 HTML 렌더
```

**역할 분담 (v5 핵심)**

| 계층 | 역할 |
|---|---|
| 외부 데이터 API | 데이터 조회·집계 + **차트별 Vega-Lite spec 완성** (그리는 방법까지 결정) |
| 프록시 JSP | **변경 없음** - 세션 체크 + JSON 패스스루. API 확정 전까지는 spec 형태의 목업 JSON 반환 |
| 브라우저 JS | 차트: `vegaEmbed(container, spec)` 렌더링 + 클릭 이벤트 수신. 테이블: HTML 렌더 + 정렬/필터 |

- 차트 모양 변경은 API(spec) 쪽에서 완결 - 프론트·프록시 재배포 불필요.
- **테이블(Pending items, by Customer)은 Vega 대상이 아님** - HTML로 렌더, 정렬·목록 필터는 클라이언트 처리 유지.
- 프론트는 받은 spec을 수정하지 않는 것이 원칙 (컨테이너 크기 맞춤 등 최소 옵션만 vegaEmbed 옵션으로).

## 2. 화면 라우팅 / 전역 셸

| URL | 화면 |
|---|---|
| `index.jsp` | → redirect `queue.jsp` (**기본 화면**) |
| `queue.jsp` | 01 Sales-Deduction Approval Queue |
| `claimLT.jsp` | 02 Claim L/T Analysis |

- 내비 메뉴 클릭 = **페이지 이동** (별도 JSP. SPA 콘텐츠 swap 아님 - 페이지 내 데이터 갱신만 리로드 없이 수행).
- 좌측 내비 레일(`sidebar.jsp`, 272px sticky): 브랜드 블록 → "DASHBOARDS" → 메뉴 2개(01엔 빨간 뱃지, 건수는 응답값). 활성: `#f4f4f4` bg + 검정 좌측 바 + 검정 인덱스 칩. 하단 안내 문구.
- 헤더(`header.jsp`): 아바타 "J" + "Hello, Jane Doe" / "Welcome back - Sales · Approver". 알림 벨 없음.

## 3. 파일 구성

```
webapp/
 ├ index.jsp / queue.jsp / claimLT.jsp        ✅ 생성됨
 ├ common/ sidebar.jsp, header.jsp, apiCommon.jsp   ✅ 생성됨
 ├ proxy/                                      ⬜ 목업(spec 형태)부터 작성
 │   ├ proxyQueue.jsp / proxyClaimDashboard.jsp
 │   ├ proxyClaimInsight.jsp / proxyClaimRawData.jsp
 └ static/js/  vega.min.js, vega-lite.min.js, vega-embed.min.js   # CDN 차단 시 로컬 배포
```

- 라이브러리: **vega + vega-lite + vega-embed 3종** (Chart.js 제거). CDN 우선, 차단 시 로컬.

## 4. 데이터 요청 설계 (Apply 없음 - 월 선택기 즉시 반영)

### 4.1 Queue - `proxy/proxyQueue.jsp?view={pending|all}&month={YYYY-MM}`

```json
{
  "asOf": "2026-06-26 02:00:41", "view": "pending", "month": "2026-06",
  "summary": {"total": 9, "risk": 5},
  "widgets": {
    "byStage":    { "$schema": ".../vega-lite/v5.json", "data": {"values": [...]},
                    "mark": "bar", "encoding": { ...스택바 spec 전체... } },
    "riskByType": { ...가로 스택바 spec 전체... }
  },
  "tables": {
    "items": [{"id": "A2S910E26540159", "stage": "CLAIM", "risks": [], "when": "06-26 16:34"}]
  }
}
```

- `widgets.*`는 그대로 `vegaEmbed`에 전달. `tables.items`는 HTML 테이블 렌더 + 클라이언트 필터(세그먼트/스테이지 select/차트 클릭) 용도로 원시 행 유지.
- 재조회 트리거: Inbox/All 토글, view=all에서 월 변경(Mar–Jun 2026, 기본 Jun; pending일 땐 dim·비활성).
- 재조회 시 각 차트는 **spec을 다시 받아 vegaEmbed 재실행** (spec이 가벼우므로 재수신·재렌더 방식 채택).
- 차트 클릭 이벤트의 datum 필드 계약은 동작 시나리오 v3.1 **§G5** 참조.

### 4.2 Claim - `proxy/proxyClaimDashboard.jsp?month={YYYY-MM}`

```json
{
  "month": "2026-06", "companyAvg": 122.4,
  "kpi": {"ltDays": 122.4, "claims": 3329},
  "widgets": {
    "monthlyTrend": { ...bar+line layer 콤보 spec... },
    "byProduct":    { ...가로 바 spec... }
  },
  "tables": {
    "steps":      [{"name": "1. Promo End ~ I/V Issue", "days": 72.7, "kind": "step"}],
    "byCustomer": [{"name": "2430031 Exertis Ireland Estore", "claims": 140,
                    "lt": 799.0, "s1": 771.3, "s2": 2.5, "s3": 5.6, "s4": 19.6}]
  }
}
```

- 월 선택기 범위: **Mar–Jun 2026, 기본 Jun** (Queue와 동일. 범위 밖 이동 불가).
- Step은 단순 라벨/값 리스트라 HTML 처리(차트 아님). byCustomer는 평균 초과 고객만, Delay 파생 컬럼(s1+s2, s3+s4)·정렬은 클라이언트.
- 월 변경 시 즉시 재조회 → spec 재embed + 테이블 tbody 재생성.

### 4.3 AI Insight - `proxy/proxyClaimInsight.jsp?month={YYYY-MM}`

- `{"summary": "..."}`. **로드 시 1회 + Refresh 시 재호출만** - 월 변경 시에는 재요청하지 않는다. 본 데이터와 분리(생성 지연이 위젯 렌더를 막지 않게). 생성 주체 TBD - 목업 텍스트.

### 4.4 공통 재렌더 규칙

- 페이지 리로드 없음: 차트는 vegaEmbed 재실행, 테이블은 tbody 재생성.
- 연타 대비 AbortController - 마지막 응답만 반영. 로딩/오류+재시도/빈 데이터 상태 필수.

## 5. 위젯 매핑 (Vega-Lite / HTML)

| 위젯 | 구현 | spec 작성 주체 |
|---|---|---|
| Queue - Pending by stage | Vega-Lite 세로 스택바 (clean `#bcd9ff` + risk `#ef3434`, 수치 라벨) | API (목업: 프록시) |
| Queue - Risk by type | Vega-Lite 가로 스택바 (스테이지 색 세그먼트 + 수치 + 합계) | API (목업: 프록시) |
| Queue - Pending items | HTML 테이블 (리스크 행 하이라이트, 칩, Open report) | - |
| Claim - Monthly Trend | Vega-Lite layer 콤보 (bar=Claims `#2f7ed8` + line=L/T `#e8863c`, 이중 축, 값 라벨) | API (목업: 프록시) |
| Claim - by Product | Vega-Lite 가로 바 (블루 셰이드 + CTV 앰버) | API (목업: 프록시) |
| Claim - Step | HTML 라벨/값 리스트 (소계 muted, Sum bold) | - |
| Claim - by Customer | HTML 테이블 (1040px 가로 스크롤, 하이라이트 컬럼, 전 컬럼 정렬) | - |

**클릭-투-필터 (Queue):** spec에 selection/클릭 시그널 정의는 **API(spec) 몫**, `result.view.addEventListener('click', ...)`로 받아 목록을 필터링하는 것은 **프론트 몫**. 상세 동작·datum 계약은 **동작 시나리오 v3.1 §G**를 따른다.

## 6. 인증·보안

프록시 JSP 첫 부분 세션 검증(미인증 401 JSON) · API 키는 서버 설정에만 · Raw Data Download 권한 체크 (TBD).

## 7. 구현 순서

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 셸 + 화면 JSP 골격 | ✅ 완료 |
| 2 | proxy 4종 **목업(spec 형태) JSON** 작성 → vegaEmbed·테이블 연결 | ⬜ 다음 |
| 3 | 동작 연결: 토글·월 선택 재조회(spec 재embed), 목록 필터, 정렬, 차트 클릭 이벤트 | ⬜ |
| 4 | AI Insight Refresh, Raw Data Download, 오류/로딩/빈 상태 | ⬜ |
| 5 | 외부 API 연동: 목업 → 실호출 교체, spec 이벤트 필드 검증, 캐시 검토 | ⬜ |

## 8. 외부 API 합의 사항 (결정 로그)

| 항목 | 결정 |
|---|---|
| 차트 렌더링 | Vega-Lite (Chart.js 폐기) |
| **spec 작성 주체** | **외부 API가 위젯별 Vega-Lite spec을 완성해 반환** (확정) |
| 테이블 데이터 | spec이 아닌 원시 행(JSON 배열)으로 반환 - 렌더·정렬·필터는 프론트 |
| 차트 클릭 datum 계약 | **잠정 정의** - 동작 시나리오 v3.1 §G5 표를 API 명세에 반영해 확정할 것 |
| 남은 협의 | 엔드포인트·인증·Raw Data·AI Insight 생성 주체·사이드바 뱃지 건수 소스(Claim 페이지)·CLAIM 스테이지 색상 |
